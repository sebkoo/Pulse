/// PulseProviders — concrete data providers over free, keyless public APIs
/// (Open-Meteo weather, USGS earthquakes). Each provider conforms to
/// `DataProvider` from PulseCore; the UI never knows which providers exist.
public enum PulseProviders {}

/// PulseUI — SwiftUI dashboard views driven entirely by `BrandConfig`.
/// Renders whatever modules the config asks for; knows nothing about
/// concrete providers beyond the `DataProvider` abstraction.
public enum PulseUI {}

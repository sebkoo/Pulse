/// PulseCore — configuration, provider abstractions, and caching.
/// UI-free by design so everything here is unit-testable on any platform.
public enum PulseCore {
    /// Semantic version of the core module.
    public static let version = "0.1.0"
}
